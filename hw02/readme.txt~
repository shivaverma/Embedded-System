----------------------------------------------------------------------------------------------------------------------------
----------------------------------------------------------------------------------------------------------------------------
 4button.js
============
	SECTION A:

 	1.The first we need to use bonescript.So import in any variable in this case it's "pointer".
	2.Using standard input/output attribute assign the pins of beagleboard.
	3.Intitially put the pins at low voltage.
	4.Read the input via pin and store in variable af array.
		a.Check for first input and using required logic gives the output.It takes input for five pins.





4button_with_Etch-sketch.js
===========================
	SECTION B:
	
	1.The first we need to use bonescript.So import in any variable in this case it's "pointer".
	2.Using standard input/output attribute assign the pins of beagleboard.
	3.Intitially put the pins at low voltage.
	4.Here there is one term called gridprint() is new which print the "X" using up,down,left,right input from pins(low/high)
		a.Check for first input and using required logic gives the output.It takes input for five pins.
